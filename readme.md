# test

## Install
`yarn add @spark/test`

## Usage
```js
import {...} from '@spark/test'
```

## Contribute
1. `yarn dev` to develop package
2. `cd test && yarn && yarn dev` to develop test

## Publish
`npm run pub`
